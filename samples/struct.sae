type struct Int {
    value i32;
}

$C++(
    #define show(it) (#it)
) exposing(show (it i32)str);

fn main() {
    let num = Int {
        value = 5
    };

    printf("%s = %d\n", show(num.value), num.value);
}