fn factorial(num i32) i32 {
    if num > 1 {
        return num * factorial(num - 1);
    } else {
        return 1;
    }
}

fn main() {
    let x = 5;

    printf("factorial(x) = %d\n", factorial(x));
}